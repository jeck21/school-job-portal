---
phase: 02-first-source-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00002_scrape_logs.sql
  - scripts/scrapers/lib/supabase-admin.ts
  - scripts/scrapers/lib/http-client.ts
  - scripts/scrapers/lib/normalizer.ts
  - scripts/scrapers/lib/school-matcher.ts
  - scripts/scrapers/lib/logger.ts
  - scripts/scrapers/lib/types.ts
  - scripts/scrapers/adapters/pareap/index.ts
  - scripts/scrapers/adapters/pareap/parser.ts
  - scripts/scrapers/adapters/pareap/types.ts
  - tests/scrapers/fixtures/pareap-listing.html
  - tests/scrapers/fixtures/pareap-detail.html
  - tests/scrapers/pareap.test.ts
  - tests/scrapers/normalizer.test.ts
  - tests/scrapers/school-matcher.test.ts
  - tests/scrapers/upsert-safety.test.ts
  - package.json
  - tsconfig.json
autonomous: true
requirements:
  - DATA-01
  - DATA-05
  - DATA-13

must_haves:
  truths:
    - "PAREAP listing pages are parsed into structured job objects with external IDs"
    - "PAREAP detail pages are parsed into full job records with location, school, certificates"
    - "Location text is decomposed into city, state, zip_code fields"
    - "School names are fuzzy-matched against existing schools table entries"
    - "Scrape runs are logged to scrape_logs table with stats and errors"
    - "Upsert operations preserve existing data when conflicts occur"
  artifacts:
    - path: "scripts/scrapers/lib/types.ts"
      provides: "SourceAdapter interface, ScrapedJob type, ScrapeResult type"
      exports: ["SourceAdapter", "ScrapedJob", "ScrapeResult", "ScrapeError"]
    - path: "scripts/scrapers/adapters/pareap/parser.ts"
      provides: "PAREAP HTML parsing for listing and detail pages"
      exports: ["parseListingPage", "parseDetailPage"]
    - path: "scripts/scrapers/lib/normalizer.ts"
      provides: "Location parsing and field normalization"
      exports: ["parseLocation", "normalizeSchoolType"]
    - path: "scripts/scrapers/lib/school-matcher.ts"
      provides: "Fuzzy school name matching against DB"
      exports: ["findOrCreateSchool"]
    - path: "scripts/scrapers/lib/logger.ts"
      provides: "Scrape run logging to scrape_logs table"
      exports: ["createScrapeLog", "updateScrapeLog"]
    - path: "scripts/scrapers/lib/http-client.ts"
      provides: "Fetch with retry, delay, and SSL handling"
      exports: ["fetchWithRetry"]
    - path: "scripts/scrapers/adapters/pareap/index.ts"
      provides: "PAREAP adapter implementing SourceAdapter interface"
      exports: ["PareapAdapter"]
    - path: "supabase/migrations/00002_scrape_logs.sql"
      provides: "scrape_logs table for run tracking"
      contains: "CREATE TABLE scrape_logs"
    - path: "tests/scrapers/pareap.test.ts"
      provides: "Unit tests for PAREAP HTML parsing"
    - path: "tests/scrapers/normalizer.test.ts"
      provides: "Unit tests for location parsing and normalization"
  key_links:
    - from: "scripts/scrapers/adapters/pareap/index.ts"
      to: "scripts/scrapers/lib/types.ts"
      via: "implements SourceAdapter interface"
      pattern: "implements SourceAdapter"
    - from: "scripts/scrapers/adapters/pareap/index.ts"
      to: "scripts/scrapers/lib/http-client.ts"
      via: "uses fetchWithRetry for page requests"
      pattern: "fetchWithRetry"
    - from: "scripts/scrapers/adapters/pareap/index.ts"
      to: "scripts/scrapers/lib/school-matcher.ts"
      via: "matches school names during job processing"
      pattern: "findOrCreateSchool"
---

<objective>
Build the PAREAP scraper adapter with all supporting infrastructure: shared types, HTTP client with retry, HTML parser, location normalizer, school fuzzy matcher, scrape logger, and the scrape_logs migration. Includes comprehensive unit tests with HTML fixtures.

Purpose: This is the core data pipeline logic. Everything needed to scrape PAREAP, parse jobs, normalize data, match schools, and write to the database -- except the CLI entrypoint and scheduling (Plan 02).
Output: Complete PAREAP adapter + shared scraper libraries + unit tests + scrape_logs migration
</objective>

<execution_context>
@/Users/cciu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cciu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-first-source-pipeline/02-CONTEXT.md
@.planning/phases/02-first-source-pipeline/02-RESEARCH.md
@.planning/phases/02-first-source-pipeline/02-VALIDATION.md

@supabase/migrations/00001_initial_schema.sql
@src/lib/supabase/client.ts
@src/lib/supabase/server.ts
@package.json
@tsconfig.json

<interfaces>
<!-- Existing database schema the scraper writes to -->

From supabase/migrations/00001_initial_schema.sql:
```sql
-- sources table: id, name, slug, base_url, scraper_type, is_active, last_scraped_at
-- schools table: id, name, district_name, school_type, address, city, state, zip_code, location, website
-- jobs table: id, source_id, school_id, district_id, external_id, title, description, url,
--   location_raw, location, city, state, zip_code, school_type, grade_band[], subject_area[],
--   salary_mentioned, salary_raw, certifications[], is_active, first_seen_at, last_verified_at, expires_at
--   UNIQUE(source_id, external_id)
-- job_sources table: id, job_id, source_id, external_id, external_url, first_seen_at, last_verified_at
--   UNIQUE(job_id, source_id)
```

From src/lib/supabase/client.ts:
```typescript
export function createBrowserClient() // Browser-side Supabase client
```

From src/lib/supabase/server.ts:
```typescript
export async function createServerClient() // Server-side Supabase client with cookie handling
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scraper types, shared libraries, migration, and test infrastructure</name>
  <files>
    scripts/scrapers/lib/types.ts,
    scripts/scrapers/lib/supabase-admin.ts,
    scripts/scrapers/lib/http-client.ts,
    scripts/scrapers/lib/normalizer.ts,
    scripts/scrapers/lib/school-matcher.ts,
    scripts/scrapers/lib/logger.ts,
    supabase/migrations/00002_scrape_logs.sql,
    tests/scrapers/normalizer.test.ts,
    tests/scrapers/school-matcher.test.ts,
    tests/scrapers/upsert-safety.test.ts,
    package.json,
    tsconfig.json
  </files>
  <behavior>
    normalizer.test.ts:
    - parseLocation("Philadelphia, PA 19102") -> { city: "Philadelphia", state: "PA", zipCode: "19102" }
    - parseLocation("Pittsburgh, PA") -> { city: "Pittsburgh", state: "PA", zipCode: undefined }
    - parseLocation("1600 Vine St. Philadelphia, PA 19102") -> { city: "Philadelphia", state: "PA", zipCode: "19102" }
    - parseLocation("") -> {}
    - normalizeSchoolType("Springfield Charter Academy") -> "charter"
    - normalizeSchoolType("Lancaster IU 13") -> "iu"
    - normalizeSchoolType("Central Dauphin SD") -> null (ambiguous, default to null)

    school-matcher.test.ts:
    - normalizeSchoolName("Spring-Ford Area School District") matches "Spring Ford Area SD" with high similarity
    - normalizeSchoolName strips "school district", "sd" abbreviations before comparison
    - findOrCreateSchool returns existing school ID when match found above threshold
    - findOrCreateSchool creates new school record when no match found

    upsert-safety.test.ts:
    - Upserting a job with same source_id+external_id updates fields, does not create duplicate
    - Upserting sets last_verified_at to current time
    - Partial batch failure does not roll back successful upserts
    - Error in one batch logs error and continues with next batch
  </behavior>
  <action>
    1. Install dependencies: `npm install cheerio string-similarity && npm install -D @types/string-similarity tsx`

    2. Update tsconfig.json: ensure `scripts/` directory is included in compilation paths. Add `"scripts"` to the `include` array if it exists, or ensure the default config covers it. The scraper uses Node.js APIs (not browser), so this may need a separate tsconfig or adjustments.

    3. Create `scripts/scrapers/lib/types.ts` with:
       - `ScrapedJob` interface: externalId, title, url, locationRaw, city?, state, zipCode?, schoolName, schoolType?, description?, subjectArea?, positionType?, certificates?: string[], deadline?, postedDate?
       - `ScrapeError` interface: message, category?, page?, timestamp
       - `ScrapeResult` interface: jobs: ScrapedJob[], errors: ScrapeError[], stats: { added, updated, skipped, failed }
       - `SourceAdapter` interface: readonly sourceSlug: string, scrape(): Promise<ScrapedJob[]>

    4. Create `scripts/scrapers/lib/supabase-admin.ts`:
       - Export `createAdminClient()` using `@supabase/supabase-js` createClient with service_role key
       - Read from `SUPABASE_URL` (fallback to `NEXT_PUBLIC_SUPABASE_URL`) and `SUPABASE_SERVICE_ROLE_KEY`
       - Disable session persistence, auto-refresh, and detectSessionInUrl
       - Throw clear error if env vars missing

    5. Create `scripts/scrapers/lib/http-client.ts`:
       - Export `fetchWithRetry(url: string, maxRetries = 3): Promise<string>`
       - Exponential backoff: 1s, 2s, 4s (capped at 10s)
       - Set User-Agent: "PAEdJobs-Bot/1.0 (+https://school-job-portal.vercel.app)"
       - Handle PAREAP SSL cert issue: use Node.js built-in fetch with custom agent or env var `NODE_TLS_REJECT_UNAUTHORIZED=0` (document this is scoped to PAREAP)
       - Export `delay(ms: number): Promise<void>` for polite scraping delays
       - Log each attempt on failure for debugging

    6. Create `scripts/scrapers/lib/normalizer.ts`:
       - Export `parseLocation(raw: string): { city?: string; state?: string; zipCode?: string }`
         Uses regex to extract City, ST ZIP from various PAREAP formats
       - Export `normalizeSchoolType(name: string): string | null`
         Keyword matching: "charter" -> "charter", "iu"/"intermediate unit" -> "iu", "cyber" -> "cyber", etc.
         Returns null when type cannot be inferred

    7. Create `scripts/scrapers/lib/school-matcher.ts`:
       - Export `normalizeSchoolName(name: string): string` -- lowercase, strip "school district", "sd", common suffixes
       - Export `findOrCreateSchool(supabase, schoolName: string, city?: string, state?: string, zipCode?: string, schoolType?: string): Promise<string>` -- returns school UUID
       - Uses string-similarity (compareTwoStrings with Dice coefficient) against existing schools
       - Threshold of 0.8 for a match
       - Creates new school record if no match, returns existing ID if matched
       - Logs near-misses (0.6-0.8 similarity) as warnings for future manual review

    8. Create `scripts/scrapers/lib/logger.ts`:
       - Export `createScrapeLog(supabase, sourceId: string): Promise<string>` -- creates "running" entry, returns log ID
       - Export `updateScrapeLog(supabase, logId: string, data: { status, jobs_added, jobs_updated, jobs_skipped, jobs_failed, errors, duration_ms })` -- updates the log entry
       - Works against `scrape_logs` table from migration

    9. Create `supabase/migrations/00002_scrape_logs.sql` per the research document:
       - scrape_logs table with: id, source_id (FK), started_at, finished_at, status, jobs_added, jobs_updated, jobs_skipped, jobs_failed, errors (JSONB), duration_ms, metadata (JSONB), created_at
       - Indexes on source_id and started_at DESC

    10. Write tests FIRST (TDD red phase), then implement:
        - `tests/scrapers/normalizer.test.ts` per behavior spec above
        - `tests/scrapers/school-matcher.test.ts` per behavior spec (mock Supabase client for DB calls)
        - `tests/scrapers/upsert-safety.test.ts` per behavior spec (mock Supabase client, test batch processing logic)
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npx vitest run tests/scrapers/normalizer.test.ts tests/scrapers/school-matcher.test.ts tests/scrapers/upsert-safety.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - All shared library files exist under scripts/scrapers/lib/
    - scrape_logs migration file exists
    - cheerio, string-similarity, tsx installed
    - normalizer tests pass (location parsing, school type detection)
    - school-matcher tests pass (name normalization, fuzzy matching, create-or-find)
    - upsert-safety tests pass (idempotent upsert, partial failure handling)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PAREAP adapter with HTML parser, fixtures, and integration tests</name>
  <files>
    scripts/scrapers/adapters/pareap/types.ts,
    scripts/scrapers/adapters/pareap/parser.ts,
    scripts/scrapers/adapters/pareap/index.ts,
    tests/scrapers/fixtures/pareap-listing.html,
    tests/scrapers/fixtures/pareap-detail.html,
    tests/scrapers/pareap.test.ts
  </files>
  <behavior>
    pareap.test.ts:
    - parseListingPage extracts job rows from fixture HTML with correct externalId, positionType, jobTitle, schoolName, location, date, detailUrl
    - parseListingPage returns empty array for HTML without #jobsrch table (structural validation)
    - parseListingPage handles missing fields gracefully (no crashes on incomplete rows)
    - parseDetailPage extracts all structured fields: Position, Subject Area, Job Title, Job Location, Deadline, Certificate(s), District, City/State/Zip
    - parseDetailPage handles multiple certificates (pipe-separated)
    - PareapAdapter.sourceSlug === "pareap"
    - getTotalPages extracts total page count from pagination "Last" link
    - Category URLs are correctly constructed with srch parameter
  </behavior>
  <action>
    1. Create test fixtures FIRST:
       - `tests/scrapers/fixtures/pareap-listing.html`: Copy a representative HTML snippet from PAREAP listing page. Must include the `#jobsrch` table with `tbody tr.jobfirstrow` rows, pagination with "Last" link, and at least 3-5 realistic job rows. Include the research document's HTML structure analysis: td:nth-child(2) for position links, td.school for school/location, td.dateTD for dates. Include the navigation pagination with page links.
       - `tests/scrapers/fixtures/pareap-detail.html`: Copy a representative HTML snippet from PAREAP detail page. Must include `table.jobpostingtable` with td.td_label/td value pairs for: Position, Subject Area, Job Title, Job Location, Deadline, Certificate, District, City/State/Zip, Telephone, Email. Include a job description section.

    2. Create `scripts/scrapers/adapters/pareap/types.ts`:
       - `PareapListingRow` interface: externalId, positionType, jobTitle, certificate, schoolName, location, date, detailUrl
       - `PareapCategory` type: { name: string, srch: string }
       - `PAREAP_CATEGORIES` const array: Teaching(100), Instructional Support(200), School Administrative(300), Support Services(400)
       - `PAREAP_BASE_URL` = "https://www.pareap.net"

    3. Create `scripts/scrapers/adapters/pareap/parser.ts`:
       - Export `parseListingPage(html: string): PareapListingRow[]`
         Uses cheerio to parse the #jobsrch table. Selects `tbody tr.jobfirstrow` rows.
         For each row: extract link href from td:nth-child(2) a, parse external ID from URL pattern /job_postings/(\d+)/, extract positionType from link text, jobTitle from link div, certificate from span, schoolName from td.school first text node, location from td.school full text, date from td.dateTD.
         MUST validate #jobsrch table exists before parsing (log warning and return [] if missing).
       - Export `parseDetailPage(html: string): Record<string, string>`
         Uses cheerio to parse table.jobpostingtable. Extracts label/value pairs from td.td_label and sibling td. Handles multiple Certificate rows by pipe-concatenating. Extracts description from jobdescription/joblisting_div sections.
       - Export `getTotalPages(html: string): number`
         Parses pagination links to find "Last" link's page parameter. Returns 1 if no pagination found.

    4. Create `scripts/scrapers/adapters/pareap/index.ts`:
       - Export `PareapAdapter` class implementing `SourceAdapter`
       - `sourceSlug = "pareap"`
       - `scrape()` method orchestrates the full pipeline:
         a. For each PAREAP_CATEGORIES: fetch first page, get total pages, fetch remaining pages (with 1-3s delay between requests using delay() from http-client)
         b. Parse each page with parseListingPage
         c. Optionally fetch detail pages for richer data (description, certificates, deadline). Use fetchWithRetry for each. Apply polite delay.
         d. Convert PareapListingRow + detail data into ScrapedJob[] using normalizer (parseLocation for structured location, normalizeSchoolType for school type inference)
         e. Collect errors in ScrapeError[] format for any failed pages/details
         f. Return ScrapedJob[] array
       - Implement expired-page detection: if all jobs on a page have dates older than 90 days, stop scraping that category (early termination per user decision). Start conservative -- log when this triggers but still stop.

    5. Write tests FIRST (TDD red), then implement:
       - `tests/scrapers/pareap.test.ts`: test parseListingPage against fixture HTML, test parseDetailPage against fixture HTML, test structural validation (missing table), test getTotalPages, test category URL construction
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npx vitest run tests/scrapers/pareap.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - PAREAP adapter exists implementing SourceAdapter interface
    - parseListingPage correctly extracts all fields from fixture HTML
    - parseDetailPage correctly extracts structured fields including multi-certificate handling
    - Structural validation returns empty array (not crash) when HTML structure unexpected
    - getTotalPages extracts page count from pagination
    - Expired-page detection logic is implemented
    - All PAREAP tests pass
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions and all new scraper tests pass:
```bash
npx vitest run --reporter=verbose
```
All tests should pass. The scraper libraries should be importable and type-check correctly.
</verification>

<success_criteria>
- PAREAP HTML is parsed into structured ScrapedJob objects with all available fields
- Location strings are decomposed into city/state/zip
- School names are fuzzy-matched with 0.8 threshold
- Upsert logic handles conflicts idempotently
- Scrape logging infrastructure records run stats
- All unit tests pass (normalizer, school-matcher, upsert-safety, pareap parser)
- Test fixtures contain realistic PAREAP HTML
</success_criteria>

<output>
After completion, create `.planning/phases/02-first-source-pipeline/02-01-SUMMARY.md`
</output>
