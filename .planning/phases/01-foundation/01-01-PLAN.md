---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/globals.css
  - src/lib/site-config.ts
  - src/lib/utils.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/components/theme-provider.tsx
  - supabase/config.toml
  - supabase/migrations/00001_initial_schema.sql
  - .env.local
  - .env.example
  - components.json
  - vitest.config.ts
  - playwright.config.ts
  - tests/unit/schema.test.ts
  - tests/e2e/landing.spec.ts
  - tests/e2e/navigation.spec.ts
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Next.js dev server starts without errors"
    - "Database migration SQL is syntactically valid and creates all required tables"
    - "Supabase client utilities export correctly"
    - "Theme system renders dark mode by default with correct CSS variables"
    - "Test infrastructure runs and produces results"
  artifacts:
    - path: "src/lib/site-config.ts"
      provides: "Centralized branding config"
      exports: ["siteConfig"]
    - path: "src/lib/supabase/client.ts"
      provides: "Browser Supabase client factory"
      exports: ["createClient"]
    - path: "src/lib/supabase/server.ts"
      provides: "Server Supabase client factory"
      exports: ["createClient"]
    - path: "supabase/migrations/00001_initial_schema.sql"
      provides: "Database schema with jobs, sources, schools, districts tables"
      contains: "CREATE TABLE jobs"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Plus Jakarta Sans font, ThemeProvider, metadata"
      exports: ["default"]
    - path: "src/app/globals.css"
      provides: "CSS variable theme system with dark slate + electric blue palette"
      contains: "--primary"
    - path: "vitest.config.ts"
      provides: "Vitest configuration for unit tests"
    - path: "playwright.config.ts"
      provides: "Playwright configuration for E2E tests"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/theme-provider.tsx"
      via: "ThemeProvider wrapping children"
      pattern: "ThemeProvider"
    - from: "src/app/layout.tsx"
      to: "src/lib/site-config.ts"
      via: "metadata import"
      pattern: "siteConfig"
    - from: "src/app/globals.css"
      to: "shadcn/ui components"
      via: "CSS variable naming convention"
      pattern: "--background.*--foreground.*--primary"
---

<objective>
Project scaffolding, database schema, theme system, and test infrastructure for the PA Educator Jobs portal.

Purpose: Establishes the entire technical foundation -- every subsequent phase builds on the project structure, theme system, database schema, and test harness created here.
Output: A running Next.js 15 dev server with Supabase client utilities, database migration, dark slate + electric blue theme, and Wave 0 test scaffolding.
</objective>

<execution_context>
@/Users/cciu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cciu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with all dependencies and configuration</name>
  <files>
    package.json, tsconfig.json, next.config.ts, components.json,
    src/lib/site-config.ts, src/lib/utils.ts,
    src/lib/supabase/client.ts, src/lib/supabase/server.ts,
    src/components/theme-provider.tsx,
    supabase/config.toml, supabase/migrations/00001_initial_schema.sql,
    .env.local, .env.example,
    vitest.config.ts, playwright.config.ts,
    tests/unit/schema.test.ts, tests/e2e/landing.spec.ts, tests/e2e/navigation.spec.ts
  </files>
  <action>
    1. Create Next.js 15 project in the current directory (not a subdirectory -- this IS the project root):
       `npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`
       If the directory is not empty, use `--yes` or handle prompts. The project name should be "school-job-portal".

    2. Install production dependencies:
       `npm install @supabase/supabase-js @supabase/ssr lucide-react next-themes`

    3. Install dev dependencies:
       `npm install -D vitest @vitejs/plugin-react playwright @playwright/test`

    4. Initialize shadcn/ui:
       `npx shadcn@latest init`
       Choose: New York style, Slate base color, CSS variables = yes.
       If interactive prompts are an issue, check for a --defaults or -y flag, or create components.json manually following shadcn/ui docs for Tailwind v4.

    5. Add shadcn/ui components needed for Phase 1:
       `npx shadcn@latest add button card`

    6. Create `src/lib/site-config.ts` -- centralized branding config:
       ```typescript
       export const siteConfig = {
         name: "PA Educator Jobs",
         tagline: "Connecting the right educators with the right jobs",
         description: "Find every relevant PA educator job opening in one place with filters that actually work.",
         url: "https://paeducatorjobs.vercel.app",
         nav: [
           { label: "Home", href: "/" },
           { label: "Jobs", href: "/jobs" },
           { label: "About", href: "/about" },
           { label: "For Schools", href: "/for-schools" },
         ],
       } as const;
       ```

    7. Create Supabase client utilities following the @supabase/ssr pattern:
       - `src/lib/supabase/client.ts`: Browser client using createBrowserClient
       - `src/lib/supabase/server.ts`: Server client using createServerClient with cookies()
       Follow the exact pattern from the RESEARCH.md code examples.

    8. Create `src/components/theme-provider.tsx` -- the "use client" wrapper for next-themes ThemeProvider. Follow the exact pattern from RESEARCH.md.

    9. Create `.env.local` and `.env.example` with:
       ```
       NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
       NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
       ```
       Ensure `.env.local` is in `.gitignore` (create-next-app should handle this).

    10. Create `supabase/` directory structure:
        - `supabase/config.toml`: Basic Supabase config (project_id = "school-job-portal")
        - `supabase/migrations/00001_initial_schema.sql`: The FULL schema from RESEARCH.md including:
          - PostGIS extension
          - sources, schools, districts, jobs, job_sources tables
          - All indexes (GIST for location, btree for common filters)
          - updated_at trigger function + triggers on all tables
          - All column types exactly as specified in RESEARCH.md

    11. Create test infrastructure (Wave 0):
        - `vitest.config.ts`: Configure with @vitejs/plugin-react, resolve alias for @/* paths
        - `playwright.config.ts`: Configure with baseURL http://localhost:3000, webServer command `npm run dev`
        - `tests/unit/schema.test.ts`: Test that reads the migration SQL file and verifies it contains CREATE TABLE statements for jobs, sources, schools, districts, job_sources. Also verify PostGIS extension is enabled. (File-based validation, not DB connection.)
        - `tests/e2e/landing.spec.ts`: Skeleton test that navigates to /, checks page title contains "PA Educator Jobs", checks for nav links. Mark as `.skip` or use a TODO comment since the page is not built yet in this task.
        - `tests/e2e/navigation.spec.ts`: Skeleton test for nav routing. Mark as `.skip` for now.

    IMPORTANT: Do NOT create src/app/layout.tsx or src/app/globals.css in this task -- those are Task 2. Let create-next-app generate defaults, Task 2 will overwrite them.
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npm run build 2>&1 | tail -5 && npx vitest run tests/unit/schema.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `npm run dev` starts without errors
    - `npm run build` succeeds
    - Schema unit test passes (migration SQL contains all required tables)
    - site-config.ts exports siteConfig with name, tagline, nav array
    - Supabase client utilities export createClient functions
    - .env.example exists with Supabase variable placeholders
  </done>
</task>

<task type="auto">
  <name>Task 2: Root layout with theme system and dark slate + electric blue palette</name>
  <files>
    src/app/layout.tsx, src/app/globals.css
  </files>
  <action>
    1. Replace `src/app/globals.css` with the full CSS variable theme system:
       - Use Tailwind v4 imports (@import "tailwindcss" or the appropriate v4 syntax -- check what create-next-app generated and follow that pattern)
       - Define CSS variables in @layer base following shadcn/ui's exact naming convention
       - Light mode (:root) and dark mode (.dark) with dark slate + electric blue palette:
         - Dark background: hsl(220, 20%, 8%)
         - Dark card: hsl(220, 18%, 12%)
         - Dark muted: hsl(220, 15%, 18%)
         - Primary (electric blue): hsl(217, 91%, 55%)
         - Border dark: hsl(220, 15%, 20%)
         - Muted foreground dark: hsl(215, 15%, 55%)
         - Foreground dark: hsl(210, 15%, 90%)
       - Set --radius: 0.625rem (10px, middle of 8-12px range per user decision)
       - Include all shadcn/ui expected variables: background, foreground, card, card-foreground, popover, popover-foreground, primary, primary-foreground, secondary, secondary-foreground, muted, muted-foreground, accent, accent-foreground, destructive, destructive-foreground, border, input, ring, chart-1 through chart-5
       - IMPORTANT: Check what Tailwind v4 / shadcn/ui generated during init and adapt accordingly. The variable format may use oklch or hsl -- match what shadcn generated.

    2. Replace `src/app/layout.tsx` with:
       - Import Plus Jakarta Sans from next/font/google with variable "--font-sans", subsets ["latin"]
       - Import ThemeProvider from @/components/theme-provider
       - Import siteConfig from @/lib/site-config for metadata
       - Import globals.css
       - Set metadata: title = siteConfig.name, description = siteConfig.description
       - Render: html (lang="en", suppressHydrationWarning) > body (font variable + font-sans + antialiased classes) > ThemeProvider (attribute="class", defaultTheme="dark", enableSystem, disableTransitionOnChange) > main with min-h-screen > {children}
       - Do NOT add Header/Footer imports yet -- those are Plan 02's job. Just render children inside ThemeProvider > main.

    3. Replace `src/app/page.tsx` with a minimal placeholder that shows "PA Educator Jobs" in a centered heading so the build and dev server work. This will be replaced in Plan 02.
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Build succeeds with no errors
    - Dev server renders the placeholder page with dark background (dark theme default)
    - globals.css contains both :root and .dark CSS variable blocks
    - layout.tsx uses Plus Jakarta Sans font, ThemeProvider with defaultTheme="dark"
    - CSS variables follow shadcn/ui naming convention exactly
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- `npx vitest run tests/unit/schema.test.ts` passes (migration SQL valid)
- Dev server (`npm run dev`) shows page with dark background and electric blue accents
- `.env.example` documents required Supabase variables
- All files exist at their expected paths
</verification>

<success_criteria>
1. Next.js 15 project builds and runs with TypeScript, Tailwind, shadcn/ui
2. Database migration SQL defines all 5 tables (sources, schools, districts, jobs, job_sources) with PostGIS
3. Dark slate + electric blue theme renders by default via CSS variables
4. Site config centralizes branding in one importable file
5. Test infrastructure (Vitest + Playwright) is configured and schema test passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
