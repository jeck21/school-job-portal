---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/jobs/page.tsx
  - src/app/about/page.tsx
  - src/app/for-schools/page.tsx
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/nav.tsx
  - src/components/coming-soon.tsx
  - src/components/landing/hero.tsx
  - src/components/landing/audience-cards.tsx
  - src/components/landing/stats-bar.tsx
  - tests/e2e/landing.spec.ts
  - tests/e2e/navigation.spec.ts
autonomous: false
requirements:
  - UI-01
  - INFRA-01

user_setup:
  - service: supabase
    why: "Database hosting for job data"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key"
    dashboard_config:
      - task: "Create a new Supabase project (free tier)"
        location: "https://supabase.com/dashboard -> New Project"
      - task: "Run initial migration via Supabase SQL Editor or CLI"
        location: "Supabase Dashboard -> SQL Editor -> paste supabase/migrations/00001_initial_schema.sql"

must_haves:
  truths:
    - "User sees a branded landing page with hero, dual-audience sections, and stats bar"
    - "User sees a header with site name/icon and navigation links"
    - "User sees a footer with copyright and site info"
    - "User can navigate to Jobs, About, and For Schools pages"
    - "Unbuilt pages (About, For Schools) show styled Coming Soon content"
    - "Jobs page shows a shell ready for Phase 3 content"
    - "The portal is publicly accessible at a URL"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Site header with logo, nav, and CTA"
      exports: ["Header"]
    - path: "src/components/layout/footer.tsx"
      provides: "Minimal site footer"
      exports: ["Footer"]
    - path: "src/components/layout/nav.tsx"
      provides: "Navigation component with links from siteConfig"
      exports: ["Nav"]
    - path: "src/components/landing/hero.tsx"
      provides: "Hero section with matchmaking messaging"
      exports: ["Hero"]
    - path: "src/components/landing/audience-cards.tsx"
      provides: "Dual-audience For Educators / For Schools section"
      exports: ["AudienceCards"]
    - path: "src/components/landing/stats-bar.tsx"
      provides: "Stats placeholders (hidden or zeros until data)"
      exports: ["StatsBar"]
    - path: "src/components/coming-soon.tsx"
      provides: "Reusable Coming Soon page component"
      exports: ["ComingSoon"]
    - path: "src/app/page.tsx"
      provides: "Landing page composing hero, audience-cards, stats-bar"
    - path: "src/app/jobs/page.tsx"
      provides: "Jobs shell page ready for Phase 3"
    - path: "src/app/about/page.tsx"
      provides: "Coming Soon about page"
    - path: "src/app/for-schools/page.tsx"
      provides: "Coming Soon for-schools page"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/header.tsx"
      via: "Header import in root layout"
      pattern: "import.*Header"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/footer.tsx"
      via: "Footer import in root layout"
      pattern: "import.*Footer"
    - from: "src/components/layout/header.tsx"
      to: "src/lib/site-config.ts"
      via: "siteConfig for nav links and site name"
      pattern: "siteConfig\\.nav"
    - from: "src/app/page.tsx"
      to: "src/components/landing/hero.tsx"
      via: "Hero component import"
      pattern: "import.*Hero"
---

<objective>
UI shell with landing page, navigation, all page routes, and Vercel deployment for the PA Educator Jobs portal.

Purpose: Delivers the visible, publicly accessible foundation -- the branded experience users and stakeholders see. Establishes the component patterns and page structure all future phases extend.
Output: A deployed portal at a public URL with branded landing page, working navigation, and Coming Soon placeholders for future pages.
</objective>

<execution_context>
@/Users/cciu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cciu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- executor needs these contracts -->

From src/lib/site-config.ts:
```typescript
export const siteConfig = {
  name: "PA Educator Jobs",
  tagline: "Connecting the right educators with the right jobs",
  description: "Find every relevant PA educator job opening in one place with filters that actually work.",
  url: "https://paeducatorjobs.vercel.app",
  nav: [
    { label: "Home", href: "/" },
    { label: "Jobs", href: "/jobs" },
    { label: "About", href: "/about" },
    { label: "For Schools", href: "/for-schools" },
  ],
} as const;
```

From src/components/theme-provider.tsx:
```typescript
export function ThemeProvider({ children, ...props }: React.ComponentProps<typeof NextThemesProvider>): JSX.Element;
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

shadcn/ui components available: Button, Card (CardHeader, CardTitle, CardDescription, CardContent)
Icons: lucide-react (Briefcase, GraduationCap, Search, MapPin, Building2, ArrowRight, Construction, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout components, page shells, and navigation</name>
  <files>
    src/components/layout/header.tsx,
    src/components/layout/footer.tsx,
    src/components/layout/nav.tsx,
    src/components/coming-soon.tsx,
    src/app/layout.tsx,
    src/app/jobs/page.tsx,
    src/app/about/page.tsx,
    src/app/for-schools/page.tsx
  </files>
  <action>
    1. Create `src/components/layout/nav.tsx`:
       - Import siteConfig.nav for link items
       - Render nav links using Next.js Link component
       - Highlight current route using usePathname()
       - "use client" since it uses usePathname hook
       - Style: text-sm font-medium, muted-foreground default, foreground on hover/active
       - Include mobile-responsive behavior (hamburger menu on small screens using a Sheet or simple toggle)

    2. Create `src/components/layout/header.tsx`:
       - Layout: logo/site name LEFT, nav CENTER, "Browse Jobs" CTA button RIGHT (per CONTEXT.md discretion: "Logo left + nav center + CTA right")
       - Logo: Simple geometric icon from lucide-react (GraduationCap or Briefcase) + styled text "PA Educator Jobs" from siteConfig.name
       - Import Nav component for center navigation
       - CTA: shadcn Button variant="default" linking to /jobs with text "Browse Jobs"
       - Style: sticky top-0, dark background with subtle border-bottom, backdrop-blur
       - "use client" if needed for mobile menu state

    3. Create `src/components/layout/footer.tsx`:
       - Minimal single-row footer (per CONTEXT.md discretion: "Minimal single-row")
       - Show: copyright year + siteConfig.name + small set of links (Home, Jobs, About)
       - Style: border-top, muted-foreground text, py-6

    4. Update `src/app/layout.tsx` (from Plan 01):
       - Add Header import above main
       - Add Footer import below main
       - Structure: ThemeProvider > div.flex.flex-col.min-h-screen > Header + main.flex-1 + Footer

    5. Create `src/components/coming-soon.tsx`:
       - Props: title (string), description (string)
       - Render: centered layout with Construction icon from lucide-react, h1 title, p description
       - Follow the exact pattern from RESEARCH.md code example

    6. Create page shells:
       - `src/app/jobs/page.tsx`: Jobs shell page with heading "Job Listings", brief text "Browse all PA educator job openings. Listings coming soon.", and a placeholder area styled as an empty state. This page will be populated in Phase 3.
       - `src/app/about/page.tsx`: Uses ComingSoon component with title="About" and description explaining the portal's mission
       - `src/app/for-schools/page.tsx`: Uses ComingSoon component with title="For Schools & Districts" and description about district features coming soon

    DESIGN REQUIREMENTS (from locked decisions):
    - Linear.app-inspired: dark, crisp, premium feel with subtle polish
    - Dark slate + electric blue palette (use the CSS variables from Plan 01)
    - Lucide icons throughout
    - Modern rounded corners (8-12px, use rounded-lg or the --radius variable)
    - Presentable MVP polish -- clean and professional, not pixel-perfect
    - NEVER hardcode "PA Educator Jobs" -- always import from siteConfig
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Build succeeds
    - All four page routes exist: /, /jobs, /about, /for-schools
    - Header renders with site name, nav links, and CTA button
    - Footer renders with copyright
    - About and For Schools pages show Coming Soon content
    - Jobs page shows empty shell
    - All text references use siteConfig imports
  </done>
</task>

<task type="auto">
  <name>Task 2: Landing page sections and E2E tests</name>
  <files>
    src/app/page.tsx,
    src/components/landing/hero.tsx,
    src/components/landing/audience-cards.tsx,
    src/components/landing/stats-bar.tsx,
    tests/e2e/landing.spec.ts,
    tests/e2e/navigation.spec.ts
  </files>
  <action>
    1. Create `src/components/landing/hero.tsx`:
       - Matchmaking messaging: headline about connecting educators with jobs (per CONTEXT.md: "connecting the right educators with the right jobs" or similar)
       - Subheadline: value proposition about aggregating PA job openings
       - Two CTA buttons: "Browse Jobs" (primary, links to /jobs) and "For Schools" (secondary/outline, links to /for-schools)
       - Style: full-width section, generous vertical padding, large bold heading (text-4xl md:text-6xl), subtle gradient or glow effect for premium feel
       - Hero messaging highlights value for BOTH audiences (educators AND districts/schools) per locked decision

    2. Create `src/components/landing/audience-cards.tsx`:
       - Two parallel cards side by side (grid, 1 col on mobile, 2 cols on md+)
       - "For Educators" card: icon (Search or Briefcase), benefits list (aggregated listings, powerful filters, fresh data, free to use)
       - "For Schools & Districts" card: icon (Building2), benefits list (reach more candidates, verified listings, manage postings, showcase district)
       - Use shadcn Card component with the dark theme styling
       - Style: cards with subtle border, hover state (slight lift or border color change), rounded-lg

    3. Create `src/components/landing/stats-bar.tsx`:
       - Stats section with 3-4 placeholder metrics: "Active Listings", "PA Sources", "Districts"
       - Values: Show "0+" or "--" with a note that real data populates after Phase 2
       - Per CONTEXT.md: "hidden or show zeros gracefully until data exists" -- show the section with graceful zero state (e.g., dashes or "Coming Soon" text for values)
       - Style: horizontal row of stats, muted styling, subtle separator between items

    4. Update `src/app/page.tsx`:
       - Compose: Hero + AudienceCards + StatsBar sections
       - Clean spacing between sections (py-16 or similar)
       - No hardcoded text -- all content in the component files

    5. Update `tests/e2e/landing.spec.ts` (un-skip and flesh out):
       - Test: page loads at /
       - Test: page title contains "PA Educator Jobs"
       - Test: hero section is visible with CTA buttons
       - Test: audience cards section is visible
       - Test: header nav links are present (Home, Jobs, About, For Schools)

    6. Update `tests/e2e/navigation.spec.ts` (un-skip and flesh out):
       - Test: clicking "Jobs" nav link navigates to /jobs
       - Test: /jobs page shows job listings heading
       - Test: /about page shows Coming Soon content
       - Test: /for-schools page shows Coming Soon content
       - Test: clicking "Browse Jobs" CTA navigates to /jobs

    7. Install Playwright browsers: `npx playwright install chromium`

    DESIGN REQUIREMENTS (from locked decisions):
    - Linear.app-inspired premium feel: subtle glows, crisp typography, dark backgrounds
    - Electric blue for primary CTAs and accents
    - Matchmaking framing for BOTH audiences
    - Stats with graceful empty state
    - Presentable MVP level
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npm run build && npx vitest run --reporter=verbose 2>&1 | tail -15 && npx playwright test --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Landing page renders hero with matchmaking messaging, audience cards, and stats bar
    - Both CTA buttons link to correct destinations
    - Dual-audience cards show benefits for educators and schools
    - Stats section shows graceful empty/zero state
    - E2E tests pass: landing page content, nav links, page routing
    - Unit tests still pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual design verification</name>
  <files>N/A</files>
  <action>
    Present the completed portal to the user for visual review. Start dev server if not running.
    The user verifies the complete Phase 1 foundation:
    1. Run `npm run dev` and open http://localhost:3000
    2. Landing page: dark slate background, electric blue accents, Linear.app-inspired premium feel
    3. Hero section: matchmaking messaging for both educators and schools, two CTA buttons
    4. Audience cards: "For Educators" and "For Schools" parallel cards with benefits
    5. Stats bar: placeholder metrics with graceful zero state
    6. Header: site name with geometric icon, centered nav (Home, Jobs, About, For Schools), "Browse Jobs" CTA
    7. Footer: minimal with copyright
    8. Navigate to /jobs -- empty shell page
    9. Navigate to /about -- styled Coming Soon page
    10. Navigate to /for-schools -- styled Coming Soon page
    11. Overall: professional, polished, trustworthy, bold and distinctive but credible
  </action>
  <verify>User visually inspects all pages at http://localhost:3000 and confirms design quality</verify>
  <done>User approves the visual design as meeting the "bold and distinctive but credible" brief with Linear.app-inspired dark aesthetic, or provides specific feedback for revision</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- `npx vitest run` -- all unit tests pass
- `npx playwright test` -- all E2E tests pass
- Landing page renders branded content with dark theme
- All 4 nav routes work (/, /jobs, /about, /for-schools)
- Coming Soon pages render for About and For Schools
- Visual inspection confirms professional, Linear.app-inspired design
</verification>

<success_criteria>
1. The portal renders a branded landing page at localhost:3000 with hero, dual-audience sections, and stats
2. Header has site name, nav links, and CTA; footer has copyright
3. All nav links route correctly; unbuilt pages show Coming Soon
4. Jobs page shows empty shell ready for Phase 3
5. Dark slate + electric blue theme is default; design feels premium and professional
6. All E2E and unit tests pass
7. Human verification confirms the visual design meets the "bold and distinctive but credible" brief
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
