---
phase: 09-operations-launch
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/monitoring.spec.ts
  - tests/e2e/performance.spec.ts
  - tests/unit/alert.test.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - UI-03

must_haves:
  truths:
    - "Wave 0 test stubs exist so that plan tasks have Nyquist-compliant verify targets"
  artifacts:
    - path: "tests/e2e/monitoring.spec.ts"
      provides: "Playwright stub for admin monitoring dashboard"
      min_lines: 10
    - path: "tests/e2e/performance.spec.ts"
      provides: "Playwright stub for search page load performance"
      min_lines: 10
    - path: "tests/unit/alert.test.ts"
      provides: "Vitest stub for scrape alert email logic"
      min_lines: 10
  key_links: []
---

<objective>
Create Wave 0 test stubs required by the phase validation strategy.

Purpose: Establish test files that later plans reference in their `<verify>` blocks, satisfying Nyquist compliance. Tests start as stubs with `.todo` markers and will be fleshed out by the plans that implement the corresponding features.

Output: Three test stub files ready to be targeted by `npx playwright test` and `npx vitest run`.
</objective>

<execution_context>
@/Users/cciu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cciu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-operations-launch/09-CONTEXT.md
@.planning/phases/09-operations-launch/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for monitoring, performance, and alerting</name>
  <files>tests/e2e/monitoring.spec.ts, tests/e2e/performance.spec.ts, tests/unit/alert.test.ts</files>
  <action>
1. Create `tests/e2e/monitoring.spec.ts` (Playwright pattern matching existing e2e tests):
   - `test.describe("Admin Monitoring Dashboard")` block with:
     - `test.todo("loads /admin/monitoring for admin user")` — INFRA-03 stub
     - `test.todo("shows scrape timeline with status dots")` — INFRA-03 stub
     - `test.todo("shows job count trend chart")` — INFRA-03 stub
     - `test.todo("shows error log with expandable details")` — INFRA-03 stub
     - `test.todo("redirects non-admin users away from /admin/monitoring")` — INFRA-03 stub
   - Import from `@playwright/test`

2. Create `tests/e2e/performance.spec.ts` (Playwright):
   - `test.describe("Search Page Performance")` block with:
     - `test.todo("search results page loads under 2 seconds")` — UI-03 stub
     - `test.todo("search with filters returns results under 2 seconds")` — UI-03 stub
   - Import from `@playwright/test`

3. Create `tests/unit/alert.test.ts` (Vitest pattern matching existing unit tests):
   - `describe("Scrape Alert Email")` block with:
     - `it.todo("sends email via Resend on failure status")` — INFRA-02 stub
     - `it.todo("sends email via Resend on partial_failure status")` — INFRA-02 stub
     - `it.todo("does not send email on success status")` — INFRA-02 stub
     - `it.todo("silently skips when RESEND_API_KEY is not configured")` — INFRA-02 stub
   - Import from `vitest`
  </action>
  <verify>
    <automated>npx vitest run tests/unit/alert.test.ts --reporter=verbose 2>&1 | tail -10 && npx playwright test tests/e2e/monitoring.spec.ts --list 2>&1 | tail -10</automated>
  </verify>
  <done>Three test stub files exist with .todo markers. Vitest and Playwright can discover them without errors. All stubs are skipped (not failing).</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/alert.test.ts` exits 0 (todos are skipped, not failures)
- `npx playwright test tests/e2e/monitoring.spec.ts --list` shows test names
- `npx playwright test tests/e2e/performance.spec.ts --list` shows test names
</verification>

<success_criteria>
1. All three test stub files exist at their expected paths
2. Test runners discover and skip the todo tests without errors
3. VALIDATION.md Wave 0 requirements are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/09-operations-launch/09-00-SUMMARY.md`
</output>
