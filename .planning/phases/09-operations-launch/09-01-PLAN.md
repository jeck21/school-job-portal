---
phase: 09-operations-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/monitoring/page.tsx
  - src/lib/queries/get-monitoring-data.ts
  - src/components/admin/scrape-timeline.tsx
  - src/components/admin/job-count-chart.tsx
  - src/components/admin/error-log-viewer.tsx
  - src/components/admin/source-summary.tsx
  - scripts/scrapers/lib/alert.ts
  - scripts/scrapers/run.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Admin can view a monitoring dashboard at /admin/monitoring showing scrape history"
    - "Dashboard shows status dots (green/yellow/red) per source over recent days"
    - "Dashboard shows a line chart of active job count trends by source"
    - "Dashboard shows expandable error details from failed/partial runs"
    - "Dashboard shows per-source summary cards with last run info"
    - "Scrape failures trigger an email alert to the operator"
    - "Non-admin users are redirected away from /admin/monitoring"
  artifacts:
    - path: "src/app/admin/monitoring/page.tsx"
      provides: "Admin monitoring dashboard page with auth gate"
      min_lines: 40
    - path: "src/lib/queries/get-monitoring-data.ts"
      provides: "Server-side queries against scrape_logs table"
      exports: ["getRecentScrapeRuns", "getJobCountTrends", "getSourceSummaries"]
    - path: "src/components/admin/scrape-timeline.tsx"
      provides: "Status dots visualization per source"
      min_lines: 30
    - path: "src/components/admin/job-count-chart.tsx"
      provides: "Recharts line chart for job count trends"
      min_lines: 25
    - path: "src/components/admin/error-log-viewer.tsx"
      provides: "Expandable error list component"
      min_lines: 25
    - path: "src/components/admin/source-summary.tsx"
      provides: "Per-source summary cards"
      min_lines: 25
    - path: "scripts/scrapers/lib/alert.ts"
      provides: "Post-scrape email alert function"
      exports: ["sendScrapeAlert"]
  key_links:
    - from: "src/app/admin/monitoring/page.tsx"
      to: "src/lib/queries/get-monitoring-data.ts"
      via: "server component data fetching"
      pattern: "getRecentScrapeRuns|getJobCountTrends|getSourceSummaries"
    - from: "src/app/admin/monitoring/page.tsx"
      to: "supabase.auth.getUser"
      via: "admin gate checking OPERATOR_EMAIL"
      pattern: "OPERATOR_EMAIL"
    - from: "scripts/scrapers/run.ts"
      to: "scripts/scrapers/lib/alert.ts"
      via: "post-scrape alert call"
      pattern: "sendScrapeAlert"
---

<objective>
Build the admin monitoring dashboard and scrape failure alerting system.

Purpose: Give the operator visibility into scrape pipeline health and immediate notification of failures -- the core operations tooling for INFRA-02 (pipeline reliability) and INFRA-03 (monitoring).

Output: Admin-only /admin/monitoring page with four views (scrape timeline, job count trends, error log, source summaries), plus email alerting on scrape failures via Resend.
</objective>

<execution_context>
@/Users/cciu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cciu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-operations-launch/09-CONTEXT.md
@.planning/phases/09-operations-launch/09-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs -->

From scripts/scrapers/lib/logger.ts:
```typescript
export async function createScrapeLog(supabase: SupabaseClient, sourceId: string): Promise<string>;
export async function updateScrapeLog(supabase: SupabaseClient, logId: string, data: {
  status: "success" | "partial_failure" | "failure";
  jobs_added?: number; jobs_updated?: number; jobs_skipped?: number; jobs_failed?: number;
  errors?: Array<{ message: string; category?: string; page?: number }>;
  duration_ms?: number;
}): Promise<void>;
```

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient();
```

From src/lib/actions/coaching-action.ts (Resend pattern):
```typescript
const resend = new Resend(apiKey);
await resend.emails.send({
  from: "PA Educator Jobs <onboarding@resend.dev>",
  to: [operatorEmail],
  subject: "...",
  html: "...",
});
```

scrape_logs table columns: id, source_id, status, started_at, finished_at, jobs_added, jobs_updated, jobs_skipped, jobs_failed, errors (JSONB), duration_ms
Sources table: id, name, slug
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monitoring data queries and alert function</name>
  <files>src/lib/queries/get-monitoring-data.ts, scripts/scrapers/lib/alert.ts</files>
  <action>
1. Create `src/lib/queries/get-monitoring-data.ts` with "use server" directive:
   - `getRecentScrapeRuns(days: number = 30)`: Query scrape_logs joined with sources (name, slug) ordered by started_at desc. Return all fields.
   - `getJobCountTrends(days: number = 30)`: Query to get daily active job counts grouped by source. Use a query that counts active jobs per day from scrape_logs (sum jobs_added - approximate, or query jobs table grouped by source with date buckets). Simpler approach: query scrape_logs grouped by date and source_id, summing jobs_added per day.
   - `getSourceSummaries()`: For each source, get the latest scrape_log entry (most recent by started_at) with status, job counts, duration, and started_at. Also get total active jobs per source from the jobs table (count where is_active = true grouped by source_id joined to sources).
   All queries use `createAdminClient()` to bypass RLS.

2. Create `scripts/scrapers/lib/alert.ts`:
   - `sendScrapeAlert(sourceName: string, status: string, errors: Array<{ message: string }>)`: Send email via Resend (per-call instantiation pattern from coaching-action.ts). Only send if status is "failure" or "partial_failure". Requires RESEND_API_KEY and OPERATOR_EMAIL env vars -- silently skip if not configured. Use HTML table format for error list in email body. From address: "PA Educator Jobs <onboarding@resend.dev>". Subject: "[Alert] Scrape {status}: {sourceName}".
  </action>
  <verify>
    <automated>npx tsx -e "import { getRecentScrapeRuns, getJobCountTrends, getSourceSummaries } from './src/lib/queries/get-monitoring-data'; console.log('Exports OK')" 2>&1 | head -5</automated>
  </verify>
  <done>Three query functions exported from get-monitoring-data.ts. Alert function exported from alert.ts with Resend integration.</done>
</task>

<task type="auto">
  <name>Task 2: Admin monitoring dashboard page with four views</name>
  <files>src/app/admin/monitoring/page.tsx, src/components/admin/scrape-timeline.tsx, src/components/admin/job-count-chart.tsx, src/components/admin/error-log-viewer.tsx, src/components/admin/source-summary.tsx</files>
  <action>
Install recharts: `npm install recharts`

1. Create `src/components/admin/source-summary.tsx` ("use client" not needed -- pure server render):
   - Accepts array of source summary data (name, status, last_run, jobs_added, jobs_updated, total_active, duration_ms)
   - Renders grid of cards (2 cols on md, 1 on mobile) with: source name, status badge (green/yellow/red based on status), last run time (relative), jobs added/updated in last run, total active jobs, duration
   - Use existing warm utility classes and Forest & Ember theme colors

2. Create `src/components/admin/scrape-timeline.tsx` ("use client"):
   - Accepts scrape run data array
   - Renders a grid: rows = sources, columns = dates (last 14 days)
   - Each cell is a colored dot: green (success), yellow (partial_failure), red (failure), gray (no run)
   - Use simple div grid with rounded-full dots, not Recharts

3. Create `src/components/admin/job-count-chart.tsx` ("use client"):
   - Accepts trend data array [{date, source, count}]
   - Uses Recharts LineChart with ResponsiveContainer (height 300)
   - One line per source with different colors
   - XAxis = date, YAxis = count, Tooltip showing source + count
   - Use theme colors: --cta for primary line, --primary for secondary

4. Create `src/components/admin/error-log-viewer.tsx` ("use client"):
   - Accepts array of scrape runs that have errors
   - Each entry: source name, date, status badge, expandable error details
   - Use HTML details/summary for expand/collapse (no extra JS needed)
   - Show up to 20 most recent error entries

5. Create `src/app/admin/monitoring/page.tsx` (server component):
   - Auth gate: get user via createClient(), check user.email === process.env.OPERATOR_EMAIL. If not admin, redirect to "/".
   - Call all three query functions from get-monitoring-data.ts
   - Render page with heading "Scrape Monitoring", then four sections:
     a. Source Summary Cards (top)
     b. Scrape Timeline (status dots grid)
     c. Job Count Trends (line chart)
     d. Recent Errors (expandable log)
   - Transform query data into the shape each component expects
   - Add metadata: title "Monitoring | PA Educator Jobs"
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Admin monitoring dashboard renders at /admin/monitoring with all four views. Non-admin users redirected. Recharts charts display scrape data.</done>
</task>

<task type="auto">
  <name>Task 3: Wire scrape failure alerts into pipeline</name>
  <files>scripts/scrapers/run.ts, .github/workflows/scrape.yml</files>
  <action>
1. Modify `scripts/scrapers/run.ts`:
   - Import `sendScrapeAlert` from `./lib/alert`
   - After each individual adapter run (both in runAll and main), check the result. If the ingest function returned a result with status "failure" or "partial_failure" (check the result object for stats where failed > 0 or errors.length > 0), call `sendScrapeAlert(adapterName, status, errors)`.
   - In the `runAll` function: after each adapter in the loop, if it threw an error (caught in catch block), call `sendScrapeAlert(name, "failure", [{ message: msg }])`.
   - In the `main` function: after single adapter run, extract status from result and call alert if needed. Wrap in try/catch so alert failure doesn't crash the scraper.

2. Update `.github/workflows/scrape.yml`:
   - Add RESEND_API_KEY and OPERATOR_EMAIL to the env block:
     ```
     RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
     OPERATOR_EMAIL: ${{ secrets.OPERATOR_EMAIL }}
     ```
   - These are already in Vercel env vars from Phase 8 coaching form. User needs to add them to GitHub repo secrets too.
  </action>
  <verify>
    <automated>npx tsx -e "import './scripts/scrapers/run'" 2>&1 | head -3</automated>
  </verify>
  <done>Scrape failures trigger email alerts. GitHub Actions workflow has access to Resend credentials. Alert failures are caught and don't crash the scraper.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- /admin/monitoring page exists and renders dashboard components
- Non-authenticated or non-admin access redirects to /
- Recharts components render in client mode with data from server queries
- Alert function sends email on failure/partial_failure status
</verification>

<success_criteria>
1. Admin monitoring dashboard accessible at /admin/monitoring with operator email gate
2. Four views render: source summary cards, scrape timeline dots, job count trend chart, error log
3. Scrape failures send email alert to operator via Resend
4. Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-operations-launch/09-01-SUMMARY.md`
</output>
