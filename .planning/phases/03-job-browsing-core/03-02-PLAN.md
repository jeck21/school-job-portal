---
phase: 03-job-browsing-core
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/layout.tsx
  - src/app/@modal/default.tsx
  - src/app/@modal/(.)jobs/[id]/page.tsx
  - src/app/jobs/page.tsx
  - src/app/jobs/[id]/page.tsx
  - src/components/jobs/job-list.tsx
  - src/components/jobs/job-row.tsx
  - src/components/jobs/job-detail.tsx
  - src/components/jobs/job-detail-modal.tsx
  - src/components/jobs/report-button.tsx
autonomous: false
requirements:
  - SRCH-01
  - SRCH-09
  - SRCH-10
  - SRCH-11

must_haves:
  truths:
    - "User sees a list of all active job postings sorted newest first"
    - "User sees total count header showing number of open positions"
    - "User can load more jobs in batches of 25"
    - "User can click a job row to open a modal with full details"
    - "Modal URL is shareable at /jobs/[id] and works on direct navigation"
    - "User can click 'Apply at [District Name]' to open original posting in new tab"
    - "User sees posted date on list rows and both posted + last verified dates in modal"
    - "User can report an issue with a job via dropdown"
    - "Empty state shows friendly message when no jobs exist"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with modal slot"
      contains: "modal"
    - path: "src/app/@modal/default.tsx"
      provides: "Default null for modal slot"
    - path: "src/app/@modal/(.)jobs/[id]/page.tsx"
      provides: "Intercepting route for job detail modal"
    - path: "src/app/jobs/page.tsx"
      provides: "Job list page with server-rendered initial batch"
    - path: "src/app/jobs/[id]/page.tsx"
      provides: "Full job detail page for direct navigation/SEO"
    - path: "src/components/jobs/job-list.tsx"
      provides: "Client component managing job list state and load more"
    - path: "src/components/jobs/job-row.tsx"
      provides: "Compact two-line job row component"
    - path: "src/components/jobs/job-detail.tsx"
      provides: "Shared job detail content (used in modal and full page)"
    - path: "src/components/jobs/job-detail-modal.tsx"
      provides: "Dialog wrapper for job detail"
    - path: "src/components/jobs/report-button.tsx"
      provides: "Report issue dropdown button"
  key_links:
    - from: "src/app/jobs/page.tsx"
      to: "src/lib/queries/get-jobs.ts"
      via: "server component import"
      pattern: "getJobs.*from.*queries/get-jobs"
    - from: "src/components/jobs/job-list.tsx"
      to: "src/lib/queries/get-jobs.ts"
      via: "server action call for load more"
      pattern: "getJobs"
    - from: "src/app/@modal/(.)jobs/[id]/page.tsx"
      to: "src/lib/queries/get-job-detail.ts"
      via: "server component import"
      pattern: "getJobDetail"
    - from: "src/components/jobs/job-detail.tsx"
      to: "job.url"
      via: "Apply CTA anchor tag"
      pattern: "Apply at.*target.*_blank"
    - from: "src/components/jobs/report-button.tsx"
      to: "src/lib/actions/report-job.ts"
      via: "server action invocation"
      pattern: "reportJob"
---

<objective>
Build the complete job browsing UI: list page, detail modal with intercepting routes, and all interactive components.

Purpose: Deliver the core browsing experience where educators can see all jobs, view details in a modal, click through to apply, and report issues.
Output: Fully functional /jobs page with list, /jobs/[id] modal overlay, load more, apply CTA, report button, and empty state.
</objective>

<execution_context>
@/Users/cciu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cciu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-job-browsing-core/03-CONTEXT.md
@.planning/phases/03-job-browsing-core/03-RESEARCH.md
@.planning/phases/03-job-browsing-core/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - data layer exports -->
From src/lib/queries/get-jobs.ts:
```typescript
export async function getJobs(offset?: number, limit?: number): Promise<{
  jobs: Array<{
    id: string; title: string; location_raw: string | null; city: string | null;
    school_type: string | null; first_seen_at: string; url: string;
    schools: { name: string; district_name: string | null } | null;
  }>;
  count: number;
}>
```

From src/lib/queries/get-job-detail.ts:
```typescript
export async function getJobDetail(id: string): Promise<{
  id: string; title: string; description: string | null; url: string;
  location_raw: string | null; city: string | null; state: string;
  school_type: string | null; salary_raw: string | null; salary_mentioned: boolean | null;
  first_seen_at: string; last_verified_at: string; is_active: boolean;
  schools: { name: string; district_name: string | null } | null;
}>
```

From src/lib/format-date.ts:
```typescript
export function formatRelativeDate(date: Date | string): string;
export function formatAbsoluteDate(date: Date | string): string;
export function formatDateDisplay(date: Date | string): string;
```

From src/lib/actions/report-job.ts:
```typescript
export async function reportJob(jobId: string, reason: "broken_link" | "filled_expired" | "other", details?: string): Promise<{ success: true }>
```

<!-- Existing UI components -->
From src/components/ui/button.tsx: Button component with variant="default"|"outline"|"ghost" and size="default"|"sm"|"lg"
From src/components/ui/dialog.tsx: Dialog, DialogContent, DialogHeader, DialogTitle (installed in Plan 01)
From src/components/ui/badge.tsx: Badge with variant="default"|"secondary"|"outline"|"destructive"
From src/components/ui/skeleton.tsx: Skeleton component
From src/components/ui/dropdown-menu.tsx: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem

<!-- Existing layout -->
From src/app/layout.tsx:
```typescript
export default function RootLayout({ children }: { children: React.ReactNode })
// Currently only accepts children -- needs modal slot added
```

<!-- Theme variables -->
- --primary = forest green (structural: icons, checkmarks, nav)
- --cta = warm amber (action buttons like "Apply")
- Dark mode default, oklch color format
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parallel route infrastructure and shared job components</name>
  <files>src/app/layout.tsx, src/app/@modal/default.tsx, src/app/@modal/(.)jobs/[id]/page.tsx, src/app/jobs/[id]/page.tsx, src/components/jobs/job-detail.tsx, src/components/jobs/job-detail-modal.tsx, src/components/jobs/report-button.tsx</files>
  <action>
    1. Update src/app/layout.tsx to accept the `modal` parallel route slot:
       - Add `modal` to the props type: `{ children: React.ReactNode; modal: React.ReactNode }`
       - Render `{modal}` after `<main>` and before `<Footer />` inside the flex container
       - Keep all existing layout structure (ThemeProvider, Header, Footer, font) intact

    2. Create src/app/@modal/default.tsx:
       ```typescript
       export default function Default() { return null; }
       ```

    3. Create src/components/jobs/job-detail.tsx -- shared detail content (used in both modal and full page):
       - Server component that receives a job object (the return type of getJobDetail)
       - Title as DialogTitle or h1 depending on context (accept `asModal` prop)
       - School name + location line
       - School type badge using Badge component with colors per type:
         - public: default variant, private: secondary, charter: outline, IU: secondary, etc. Use discretion for colors
       - "Posted X days ago (Mon D, YYYY)" for first_seen_at using formatDateDisplay
       - "Last verified X days ago (Mon D, YYYY)" for last_verified_at using formatDateDisplay
       - Description section (render as prose, handle null gracefully)
       - Salary section if salary_raw exists
       - "Apply at [District Name]" CTA button:
         - Use warm amber CTA variant (bg-cta text-cta-foreground)
         - `href={job.url}` with `target="_blank"` and `rel="noopener noreferrer"`
         - Fall back to "Apply at District" if district_name is null
       - ReportButton component at bottom

    4. Create src/components/jobs/report-button.tsx -- client component:
       - DropdownMenu with trigger button labeled "Report an issue" (use Flag icon from lucide-react)
       - Three dropdown items: "Link is broken", "Job is filled/expired", "Other"
       - On click, calls reportJob server action with the appropriate reason
       - Shows brief success feedback (text change to "Reported" with check icon, resets after 2s)
       - Accept `jobId: string` prop

    5. Create src/components/jobs/job-detail-modal.tsx -- client component:
       - Uses Dialog from shadcn with `defaultOpen={true}` (try this first; if Base UI doesn't support it, use controlled `open={true}`)
       - `onOpenChange`: when closed, call `router.back()`
       - DialogContent with `max-w-2xl max-h-[85vh] overflow-y-auto`
       - Renders JobDetail inside with `asModal={true}`

    6. Create src/app/@modal/(.)jobs/[id]/page.tsx -- intercepting route:
       - Server component, awaits params to get `id`
       - Calls getJobDetail(id), handles not found
       - Renders JobDetailModal wrapping JobDetail with the fetched data
       - NOTE: Use `(.)` not `(..)` -- @modal is a slot not a segment

    7. Create src/app/jobs/[id]/page.tsx -- full page fallback for direct navigation:
       - Server component, awaits params to get `id`
       - Calls getJobDetail(id), handles not found with notFound()
       - Renders JobDetail with `asModal={false}` in a centered container (max-w-2xl mx-auto)
       - Add metadata generation for SEO (title = job title)
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Root layout accepts modal slot. @modal parallel route and intercepting route exist. Job detail renders in both modal and full-page contexts. Report button submits flags. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Job list page with load more, rows, empty state, and count header</name>
  <files>src/app/jobs/page.tsx, src/components/jobs/job-list.tsx, src/components/jobs/job-row.tsx</files>
  <action>
    1. Create src/components/jobs/job-row.tsx -- client component for a single list row:
       - Wraps in Next.js `<Link href={/jobs/${job.id}}>` for interception to work
       - Compact two-line layout per user decision:
         - Line 1: Title (font-medium) centered-dot School Name centered-dot Location (city or location_raw)
         - Line 2: School type badge (small, using Badge with "secondary" variant and appropriate styling per type) + "Posted X ago" using formatRelativeDate
       - Hover state: subtle background change (bg-muted/50)
       - Handle null school gracefully (show "School" placeholder)
       - Handle null location gracefully (omit the dot + location portion)
       - Keep the row visually tight -- py-3 px-4 with border-b for separation

    2. Create src/components/jobs/job-list.tsx -- client component:
       - Receives initial jobs array and total count as props (from server component)
       - State: `jobs` array (initialized with props), `hasMore` boolean
       - "Load more" button at bottom:
         - Calls getJobs(currentOffset, 25) server action
         - Appends results to existing jobs array
         - Hides button when all jobs loaded (jobs.length >= count)
         - Shows loading spinner/state while fetching
       - Renders count header: "{count} open positions" (or "1 open position" singular)
       - Maps over jobs rendering JobRow for each
       - Loading skeleton: show 5 skeleton rows on initial client hydration if needed

    3. Replace src/app/jobs/page.tsx -- server component:
       - Calls getJobs(0, 25) server-side for initial render
       - If jobs array is empty, render empty state:
         - Briefcase icon (already imported in current shell)
         - "No job listings yet -- we're actively collecting PA educator positions. Check back soon!"
         - Friendly, honest per user decision
       - If jobs exist, render JobList with initial data and count
       - Page title: "Job Listings" with tagline
       - Keep max-w-6xl container with px-4 py-8 (tighter than current py-16)

    4. Verify the full flow works:
       - Start dev server on port 3003
       - Check /jobs loads (may show empty state if no data in DB, which is correct)
       - Check build passes
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Job list page renders with count header, compact rows, load more button, and empty state. Each row links to /jobs/[id] for modal interception. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete browsing experience</name>
  <files>n/a</files>
  <action>
    Human verifies the complete job browsing flow visually. Dev server should already be running from Task 2.

    What was built: Complete job browsing experience -- list page with compact rows, detail modal with intercepting routes, apply CTA, report button, load more, empty state, and date formatting.

    Steps to verify:
    1. Run `npm run dev -- -p 3003` and visit http://localhost:3003/jobs
    2. If jobs exist in DB:
       - Verify compact list rows show Title, School, Location on line 1 and school type badge + "Posted X ago" on line 2
       - Verify count header shows "X open positions"
       - Click a job row -- modal should open with full detail overlay
       - Check URL changes to /jobs/[id]
       - Verify modal shows title, school, location, description, dates (posted + last verified in relative + absolute format)
       - Verify "Apply at [District Name]" button opens original posting in new tab
       - Verify "Report an issue" dropdown has 3 options
       - Close modal with X or clicking outside -- should return to list
       - Copy /jobs/[id] URL and open in new tab -- should render full page version
       - If more than 25 jobs, verify "Load more" button works
    3. If no jobs in DB:
       - Verify empty state message: "No job listings yet..."
    4. Check basic mobile appearance (resize browser narrow)
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User approves the visual appearance and interactive behavior of job list and detail modal.</done>
</task>

</tasks>

<verification>
- `npm run build` -- build succeeds with no errors
- `npx vitest run` -- all unit tests pass (format-date from Plan 01)
- /jobs page renders job list or empty state
- /jobs/[id] modal opens from list click with correct URL
- /jobs/[id] direct navigation renders full page
- Apply button links to job.url with target="_blank"
- Report dropdown submits to report_flags table
- Dates show relative + absolute format per user decision
</verification>

<success_criteria>
- User sees all active jobs in a compact list sorted newest first
- User sees total count of open positions
- User can click a job to open detail modal with shareable URL
- User can click "Apply at [District Name]" to reach original posting
- User sees posted date on rows, both dates in modal (relative + absolute)
- User can report issues via dropdown without login
- Empty state displays when no jobs exist
- Load more appends next 25 jobs
</success_criteria>

<output>
After completion, create `.planning/phases/03-job-browsing-core/03-02-SUMMARY.md`
</output>
