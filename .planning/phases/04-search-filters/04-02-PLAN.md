---
phase: 04-search-filters
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/layout.tsx
  - src/lib/hooks/use-job-filters.ts
  - src/components/jobs/search-filter-bar.tsx
  - src/components/jobs/filter-dropdown.tsx
  - src/components/jobs/radius-filter.tsx
  - src/components/jobs/active-filters.tsx
  - src/components/jobs/job-list.tsx
  - src/app/jobs/page.tsx
autonomous: true
requirements:
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - SRCH-07
  - SRCH-08
  - SRCH-12

must_haves:
  truths:
    - "User can type keywords and see results filtered by title, school name, or location"
    - "User can enter a PA zip code and set a radius slider to filter by distance"
    - "User can select school types, grade bands, subjects, and cert types from multi-select dropdowns"
    - "User can toggle Salary Info Included to show only salary-mentioning jobs"
    - "User can toggle Include Unspecified Postings to control whether jobs with missing data appear"
    - "User can combine any set of filters and see the intersection of results"
    - "Filter state is reflected in URL query params and survives page refresh"
    - "User can clear all filters at once"
  artifacts:
    - path: "src/lib/hooks/use-job-filters.ts"
      provides: "nuqs-based filter state hook"
      exports: ["useJobFilters"]
    - path: "src/components/jobs/search-filter-bar.tsx"
      provides: "Search input + filter dropdowns + toggles"
      min_lines: 50
    - path: "src/components/jobs/filter-dropdown.tsx"
      provides: "Reusable multi-select dropdown component"
      min_lines: 30
    - path: "src/components/jobs/radius-filter.tsx"
      provides: "Zip input + radius slider"
      min_lines: 30
    - path: "src/components/jobs/job-list.tsx"
      provides: "Updated job list using searchJobs with filter state"
      min_lines: 50
    - path: "src/app/jobs/page.tsx"
      provides: "Jobs page passing searchParams to filter bar and initial query"
      min_lines: 20
  key_links:
    - from: "src/components/jobs/search-filter-bar.tsx"
      to: "src/lib/hooks/use-job-filters.ts"
      via: "useJobFilters() hook call"
      pattern: "useJobFilters"
    - from: "src/components/jobs/job-list.tsx"
      to: "src/lib/queries/search-jobs.ts"
      via: "searchJobs() server action call"
      pattern: "searchJobs"
    - from: "src/lib/hooks/use-job-filters.ts"
      to: "URL query params"
      via: "nuqs useQueryStates"
      pattern: "useQueryStates"
    - from: "src/app/layout.tsx"
      to: "nuqs"
      via: "NuqsAdapter wrapper"
      pattern: "NuqsAdapter"
---

<objective>
Build the complete filter UI and wire it to the search_jobs backend: search bar, filter dropdowns (school type, grade band, subject, certification), salary toggle, include-unspecified toggle, zip+radius controls, active filter chips, and URL state management via nuqs. Update job-list to use the searchJobs action with all filter params.

Purpose: This delivers the entire user-facing search and filter experience. Educators can narrow thousands of jobs to exactly what they need using any combination of filters.

Output: Filter bar components, nuqs integration, updated job list and page with filter support.
</objective>

<execution_context>
@/Users/cciu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cciu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-search-filters/04-CONTEXT.md
@.planning/phases/04-search-filters/04-RESEARCH.md
@.planning/phases/04-search-filters/04-01-SUMMARY.md

@src/app/layout.tsx
@src/app/jobs/page.tsx
@src/components/jobs/job-list.tsx
@src/components/jobs/job-row.tsx
@src/lib/queries/get-jobs.ts

<interfaces>
<!-- From Plan 01 outputs -->
From src/lib/queries/search-jobs.ts:
```typescript
"use server";
export type JobFilters = {
  q?: string;
  type?: string[];
  grade?: string[];
  subject?: string[];
  cert?: string[];
  salary?: boolean;
  zip?: string;
  radius?: number;
  unspecified?: boolean;
};
export async function searchJobs(filters: JobFilters, offset?: number, limit?: number): Promise<{ jobs: any[]; count: number }>;
```

From src/lib/filter-options.ts:
```typescript
export const SCHOOL_TYPES: readonly { value: string; label: string }[];
export const GRADE_BANDS: readonly { value: string; label: string }[];
export const SUBJECT_AREAS: readonly { value: string; label: string }[];
export const CERTIFICATION_TYPES: readonly { value: string; label: string }[];
export const RADIUS_OPTIONS: { min: 5; max: 150; step: 5; default: 25 };
```

From src/components/jobs/job-list.tsx (existing):
```typescript
type JobData = {
  id: string; title: string; location_raw: string | null;
  city: string | null; school_type: string | null;
  first_seen_at: string; url: string;
  schools: { name: string; district_name: string | null } | null;
};
export function JobList({ initialJobs, totalCount }: { initialJobs: JobData[]; totalCount: number });
```

From src/app/layout.tsx (existing):
```typescript
// Wraps children in ThemeProvider. Must add NuqsAdapter inside ThemeProvider.
export default function RootLayout({ children, modal }: { children: React.ReactNode; modal: React.ReactNode });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn components, NuqsAdapter, and useJobFilters hook</name>
  <files>src/app/layout.tsx, src/lib/hooks/use-job-filters.ts</files>
  <action>
1. Add required shadcn/ui components that are missing:
   ```bash
   npx shadcn@latest add slider popover command checkbox switch label separator --yes
   ```

2. Update `src/app/layout.tsx`:
   - Import NuqsAdapter from 'nuqs/adapters/next/app'
   - Wrap children inside NuqsAdapter WITHIN the existing ThemeProvider
   - Keep all existing structure (Header, Footer, modal slot, ThemeProvider) intact
   - The NuqsAdapter wraps the div.flex.min-h-screen content

3. Create `src/lib/hooks/use-job-filters.ts`:
   - "use client" directive (this is a client hook)
   - Import from 'nuqs': useQueryStates, parseAsArrayOf, parseAsString, parseAsBoolean, parseAsInteger
   - Export useJobFilters() hook that returns useQueryStates with:
     - q: parseAsString.withDefault('')
     - type: parseAsArrayOf(parseAsString, ',').withDefault([])
     - grade: parseAsArrayOf(parseAsString, ',').withDefault([])
     - subject: parseAsArrayOf(parseAsString, ',').withDefault([])
     - cert: parseAsArrayOf(parseAsString, ',').withDefault([])
     - salary: parseAsBoolean.withDefault(false)
     - zip: parseAsString.withDefault('')
     - radius: parseAsInteger.withDefault(25)
     - unspecified: parseAsBoolean.withDefault(true)
   - Use shallow: true option to avoid full page re-renders
   - Export a clearAllFilters helper that resets all params to defaults
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/hooks/use-job-filters.ts 2>&1 | head -10; grep "NuqsAdapter" src/app/layout.tsx</automated>
  </verify>
  <done>shadcn slider/popover/command/checkbox/switch components installed, NuqsAdapter wraps app, useJobFilters hook manages all filter params as URL state</done>
</task>

<task type="auto">
  <name>Task 2: Build search-filter-bar with all filter controls</name>
  <files>src/components/jobs/search-filter-bar.tsx, src/components/jobs/filter-dropdown.tsx, src/components/jobs/radius-filter.tsx, src/components/jobs/active-filters.tsx</files>
  <action>
1. Create `src/components/jobs/filter-dropdown.tsx`:
   - "use client" component
   - Props: { label: string, options: {value: string, label: string}[], selected: string[], onChange: (values: string[]) => void }
   - Uses shadcn Popover + Command (combobox pattern) for multi-select
   - Shows selected count badge when items are selected (e.g., "Type (2)")
   - Each option has a Checkbox next to it
   - Searchable list for dropdowns with many options (subject areas, cert types)
   - Close popover on click outside
   - Style with Forest & Ember theme: use --primary for selected states, clean dark backgrounds

2. Create `src/components/jobs/radius-filter.tsx`:
   - "use client" component
   - Props: { zip: string, radius: number, onZipChange: (zip: string) => void, onRadiusChange: (radius: number) => void }
   - Uses shadcn Popover as container (matches filter-dropdown pattern)
   - Inside popover: text input for 5-digit zip code (pattern validates PA zips), shadcn Slider for radius (5-150 miles, step 5)
   - Show current radius value label (e.g., "25 mi")
   - Button label shows "Location" or "Location (zip, radius mi)" when active
   - Validate zip is 5 digits before accepting

3. Create `src/components/jobs/active-filters.tsx` (Claude's discretion: implementing filter chips):
   - "use client" component
   - Props: { filters: from useJobFilters, onRemove: handler to clear individual filter }
   - Renders removable Badge chips for each active filter
   - Maps filter values to labels using filter-options.ts constants
   - Shows nothing when no filters are active

4. Create `src/components/jobs/search-filter-bar.tsx`:
   - "use client" component
   - Uses useJobFilters() hook for state
   - Uses useDebouncedCallback from 'use-debounce' for search input (300ms)
   - Layout per user decision: collapsible top bar with horizontal arrangement
     - Search bar: full width at top, with Search icon (lucide) and clear (X) button
     - Filter row below search: Type, Grade, Zip+Radius, Subject, Salary toggle, Cert dropdowns in a horizontal row
     - "Include unspecified" toggle (shadcn Switch) at the end of filter row
     - "Clear all" link/button to reset all filters
   - Each filter uses FilterDropdown or RadiusFilter or Switch components
   - Filters apply instantly on selection (no Apply button) -- nuqs handles URL sync
   - Active filter chips (ActiveFilters) rendered below filter bar when any filter is active
   - Responsive: on smaller screens, filter row wraps. Full mobile polish is Phase 8 but make it basically usable.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/jobs/search-filter-bar.tsx src/components/jobs/filter-dropdown.tsx src/components/jobs/radius-filter.tsx src/components/jobs/active-filters.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Search bar with debounced input, 6 filter controls (type, grade, zip+radius, subject, salary toggle, cert), include-unspecified toggle, clear-all button, and active filter chips all render in a horizontal bar layout</done>
</task>

<task type="auto">
  <name>Task 3: Wire filters to job-list and update jobs page</name>
  <files>src/components/jobs/job-list.tsx, src/app/jobs/page.tsx</files>
  <action>
1. Update `src/components/jobs/job-list.tsx`:
   - Import useJobFilters from hooks
   - Import searchJobs from queries/search-jobs
   - Add filter state awareness: when filters change, reset jobs list and fetch from offset 0
   - Use useEffect watching filter state to trigger new searches
   - The loadMore function must pass current filter state to searchJobs (not getJobs)
   - When filters are active, use searchJobs(filters, offset, limit)
   - When no filters active (all defaults), still use searchJobs for consistency
   - Reset pagination (offset=0, clear accumulated jobs) when any filter value changes
   - Show loading skeleton during filter transitions
   - Update the JobData type to accommodate the fields returned by search_jobs RPC (school_name, district_name as flat fields instead of nested schools object)
   - Handle the mapping: RPC returns school_name/district_name flat, but JobRow expects schools: {name, district_name}. Map in the component or adjust JobRow.

2. Update `src/app/jobs/page.tsx`:
   - Import SearchFilterBar component
   - Read searchParams from the page props (Next.js App Router passes these to server components)
   - For SSR initial load: parse searchParams and call searchJobs with them for the initial data
   - Pass initialJobs and totalCount to JobList
   - Render SearchFilterBar above JobList
   - Use Suspense boundary around JobList for loading states
   - Import nuqs createSearchParamsCache for server-side param parsing to avoid hydration mismatches (see Pitfall 4 in RESEARCH.md)
   - Keep the empty state UI when no jobs match filters (update message to "No jobs match your filters" when filters are active vs "No job listings yet" when no data)

IMPORTANT: Pagination + filters pitfall (from RESEARCH.md Pitfall 5): Every loadMore call MUST include the current filter state. When filters change, reset to page 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/jobs/job-list.tsx src/app/jobs/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Jobs page shows search bar + filter panel above job list, filters narrow results in real-time, URL reflects filter state, pagination works correctly with active filters, page refresh preserves filter selections</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- Filter bar renders search input + all 6 filter controls + toggles
- Selecting a filter updates URL params (e.g., /jobs?type=public)
- Searching by keyword filters the job list after 300ms debounce
- Combining multiple filters narrows results (AND logic)
- "Clear all" resets all filters and URL to /jobs
- Load more continues to work with active filters
- Page refresh with filter URL params shows filtered results
- Include Unspecified toggle changes result set when enabled/disabled
</verification>

<success_criteria>
- SRCH-02: Keyword search filters by title, school name, location
- SRCH-03: Zip code + radius slider filters jobs by distance
- SRCH-04: School type multi-select filters correctly
- SRCH-05: Grade band multi-select filters correctly
- SRCH-06: Subject/position multi-select filters correctly
- SRCH-07: Salary toggle shows only salary-mentioning jobs
- SRCH-08: Certification multi-select filters correctly
- SRCH-12: Multiple filters combine with AND logic, all reflected in URL
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-filters/04-02-SUMMARY.md`
</output>
