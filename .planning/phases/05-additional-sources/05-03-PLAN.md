---
phase: 05-additional-sources
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - .github/workflows/scrape.yml
  - scripts/scrapers/adapters/pareap/ingest.ts
  - package.json
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-07

must_haves:
  truths:
    - "All 4 adapters run successfully via CLI and produce jobs"
    - "Unified GitHub Actions workflow runs each adapter on a staggered daily schedule"
    - "PAREAP adapter uses the shared ingestion pipeline (refactored from custom ingest)"
    - "NODE_TLS_REJECT_UNAUTHORIZED=0 only applies to PAREAP, not other adapters"
    - "workflow_dispatch allows running any single adapter or all adapters manually"
  artifacts:
    - path: ".github/workflows/scrape.yml"
      provides: "Unified workflow with staggered cron schedules for all 4 sources"
      contains: "paeducator"
    - path: "scripts/scrapers/adapters/pareap/ingest.ts"
      provides: "Refactored PAREAP ingest using shared pipeline"
  key_links:
    - from: ".github/workflows/scrape.yml"
      to: "scripts/scrapers/run.ts"
      via: "npx tsx scripts/scrapers/run.ts {adapter}"
      pattern: "run\\.ts"
    - from: "scripts/scrapers/adapters/pareap/ingest.ts"
      to: "scripts/scrapers/lib/ingest-pipeline.ts"
      via: "runIngestion call"
      pattern: "runIngestion"
---

<objective>
Unify the GitHub Actions workflow for all 4 sources with staggered schedules, refactor PAREAP to use the shared pipeline, and verify end-to-end adapter execution.

Purpose: This plan wires everything together. The unified workflow file (user decision: single file for all sources) replaces the existing PAREAP-only workflow, adding staggered cron schedules. PAREAP is refactored to use the shared ingestion pipeline for consistency. End-to-end dry-run verification ensures all adapters work.
Output: Production-ready unified workflow, all adapters using shared pipeline, verified CLI execution.
</objective>

<execution_context>
@/Users/cciu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cciu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-additional-sources/05-CONTEXT.md
@.planning/phases/05-additional-sources/05-RESEARCH.md
@.planning/phases/05-additional-sources/05-01-SUMMARY.md
@.planning/phases/05-additional-sources/05-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From scripts/scrapers/lib/ingest-pipeline.ts:
```typescript
export async function runIngestion(
  adapter: SourceAdapter,
  sourceConfig: { name: string; slug: string; baseUrl: string; scraperType: string }
): Promise<ScrapeResult>;
```

<!-- From Plan 01/02 - run.ts after both plans -->
From scripts/scrapers/run.ts:
```typescript
const ADAPTERS: Record<string, () => Promise<unknown>> = {
  pareap: ingestPareap,
  paeducator: ingestPaeducator,
  schoolspring: ingestSchoolSpring,
  teachingjobsinpa: ingestTeachingJobsInPA,
};
```

<!-- Existing PAREAP adapter to refactor -->
From scripts/scrapers/adapters/pareap/ingest.ts:
```typescript
export async function ingestPareap(): Promise<ScrapeResult>;
// Currently has its own ensureSource, processBatch, etc.
// Refactor to use runIngestion from shared pipeline
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor PAREAP to shared pipeline + unified workflow</name>
  <files>
    scripts/scrapers/adapters/pareap/ingest.ts,
    .github/workflows/scrape.yml,
    package.json
  </files>
  <action>
    1. Refactor `scripts/scrapers/adapters/pareap/ingest.ts`:
       - Replace the custom ingestion logic (ensureSource, processBatch, getExistingExternalIds, etc.) with a call to `runIngestion` from the shared pipeline
       - `ingestPareap()` becomes: `runIngestion(new PareapAdapter(), {name: "PAREAP", slug: "pareap", baseUrl: "https://www.pareap.net", scraperType: "cheerio"})`
       - Ensure the PareapAdapter import and shared pipeline import are correct
       - Keep the function signature the same (`export async function ingestPareap(): Promise<ScrapeResult>`) so run.ts does not need changes

    2. Replace `.github/workflows/scrape.yml` with unified workflow:
       - Name: "Scrape Job Sources"
       - Staggered cron schedules per user decision:
         - `0 6 * * *` -- PAREAP (6 AM UTC, existing schedule)
         - `0 10 * * *` -- PAeducator (10 AM UTC)
         - `0 14 * * *` -- SchoolSpring (2 PM UTC)
         - `0 18 * * *` -- TeachingJobsInPA (6 PM UTC)
       - workflow_dispatch with adapter input (choice: pareap, paeducator, schoolspring, teachingjobsinpa, all)
       - Single job "scrape" with steps:
         a. checkout, setup-node, npm ci
         b. "Determine adapter" step: map `github.event.schedule` to adapter name, or use `github.event.inputs.adapter` for manual dispatch
         c. Run adapter: `npx tsx scripts/scrapers/run.ts $ADAPTER_NAME`
         d. For "all": run each adapter sequentially with 30s delays between
       - Env vars: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY from secrets
       - CRITICAL: `NODE_TLS_REJECT_UNAUTHORIZED=0` must ONLY be set for the PAREAP step, NOT globally. Use conditional env or set it only when adapter is "pareap" or "all" (for PAREAP portion)
       - timeout-minutes: 30 (increased from 15 to accommodate multiple adapters for "all" runs)

    3. Ensure all npm scripts exist in package.json:
       - `scrape:pareap`: `npx tsx scripts/scrapers/run.ts pareap`
       - `scrape:paeducator`: `npx tsx scripts/scrapers/run.ts paeducator`
       - `scrape:schoolspring`: `npx tsx scripts/scrapers/run.ts schoolspring`
       - `scrape:teachingjobsinpa`: `npx tsx scripts/scrapers/run.ts teachingjobsinpa`
       - `scrape:all`: `npx tsx scripts/scrapers/run.ts all`

    4. Update run.ts to support "all" command: when adapterName is "all", run each adapter sequentially with error isolation (one adapter failure should not prevent others from running). Log results for each.
  </action>
  <verify>
    <automated>npx vitest run tests/scrapers/ --reporter=verbose && npx tsx scripts/scrapers/run.ts 2>&1 | grep -q "paeducator" && npx tsx scripts/scrapers/run.ts 2>&1 | grep -q "schoolspring" && npx tsx scripts/scrapers/run.ts 2>&1 | grep -q "teachingjobsinpa" && echo "All adapters registered"</automated>
  </verify>
  <done>PAREAP uses shared pipeline. Unified workflow has 4 staggered cron schedules. NODE_TLS_REJECT_UNAUTHORIZED=0 is PAREAP-only. All 4 adapters listed in run.ts help output. npm scripts exist for each adapter and "all".</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/scrapers/ --reporter=verbose` -- all tests still pass after PAREAP refactor
- `npx tsx scripts/scrapers/run.ts` -- shows all 4 adapters in help output
- `.github/workflows/scrape.yml` contains 4 cron entries with staggered times
- `NODE_TLS_REJECT_UNAUTHORIZED` is only set for PAREAP runs
- `npm run scrape:paeducator` command exists (dry-run test -- actual execution requires Supabase credentials)
</verification>

<success_criteria>
- Unified workflow replaces PAREAP-only workflow with all 4 sources
- PAREAP refactored to shared pipeline without behavior change
- All adapters callable via CLI and npm scripts
- "all" command runs every adapter sequentially with error isolation
- Existing tests still pass after refactor
</success_criteria>

<output>
After completion, create `.planning/phases/05-additional-sources/05-03-SUMMARY.md`
</output>
