---
phase: 08-ui-polish-static-pages
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - src/app/globals.css
  - src/components/landing/hero.tsx
  - src/components/landing/audience-cards.tsx
  - src/components/landing/stats-bar.tsx
  - src/components/jobs/job-row.tsx
  - src/components/jobs/job-list.tsx
  - src/app/about/page.tsx
  - src/app/coaching/page.tsx
  - src/app/coaching/coaching-form.tsx
  - src/components/layout/header.tsx
autonomous: false
requirements: [UI-02]

must_haves:
  truths:
    - "Interactive elements have warm hover/active states (amber glow, smooth transitions)"
    - "Cards and sections have subtle warm accent backgrounds or gradient washes"
    - "Micro-interactions make the site feel alive (hover transitions, focus states, subtle animations)"
    - "Both light and dark modes look polished with consistent warm accents"
    - "All pages (landing, jobs, about, coaching, district pages) have consistent visual warmth"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Warm gradient utility classes, transition utilities, accent CSS variables"
      contains: "warm-glow"
    - path: "src/components/landing/hero.tsx"
      provides: "Polished hero with warm accents and micro-interactions"
    - path: "src/components/landing/audience-cards.tsx"
      provides: "Audience cards with warm hover effects"
  key_links:
    - from: "src/app/globals.css"
      to: "All components"
      via: "CSS custom properties and utility classes"
      pattern: "warm-glow|--warm"
---

<objective>
Apply visual polish pass across all pages to add warmth, personality, and micro-interactions that make the site feel handcrafted rather than AI-generated.

Purpose: The user specifically noted the site feels "a little generic" and "AI-generated." This pass adds warm accents, hover effects, transitions, and visual personality across every page to make it feel alive and intentional.
Output: Polished, warm visual experience across all pages in both light and dark modes.
</objective>

<execution_context>
@/Users/cciu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cciu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-polish-static-pages/08-CONTEXT.md
@.planning/phases/08-ui-polish-static-pages/08-RESEARCH.md
@.planning/phases/08-ui-polish-static-pages/08-01-SUMMARY.md
@.planning/phases/08-ui-polish-static-pages/08-02-SUMMARY.md

<interfaces>
From src/app/globals.css (existing theme):
```css
/* Light mode */
:root {
  --cta: oklch(0.72 0.13 70);           /* warm amber */
  --cta-foreground: oklch(0.15 0.02 70);
  --primary: oklch(0.55 0.14 160);       /* forest green */
  --primary-foreground: oklch(1 0 0);
}
/* Dark mode uses similar structure with adjusted lightness */
```

Tailwind theme maps:
- `bg-cta` / `text-cta-foreground` for warm amber buttons
- `bg-primary` / `text-primary-foreground` for forest green structural elements
- oklch color format throughout
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global warm utilities and component polish</name>
  <files>src/app/globals.css, src/components/landing/hero.tsx, src/components/landing/audience-cards.tsx, src/components/landing/stats-bar.tsx, src/components/jobs/job-row.tsx, src/components/jobs/job-list.tsx, src/components/layout/header.tsx</files>
  <action>
**globals.css — Add warm utility classes and transitions:**
- Add `.warm-glow` class: subtle radial gradient with warm amber tint (oklch 0.76 0.08 70 at ~6% opacity for light, ~8% for dark)
- Add `.warm-glow-hover` for interactive elements: warm amber glow on hover via box-shadow or background transition
- Add smooth transition defaults: `transition-all duration-200 ease-out` as a utility pattern
- Add warm accent CSS custom properties if needed (e.g., `--warm-accent` for a softer warm tone usable in backgrounds)
- Ensure all warm values have both `:root` and `.dark` variants

**Landing page — hero.tsx:**
- Add a subtle warm radial gradient wash behind the hero content (warm-glow positioned as a decorative background)
- Add smooth hover transition on the CTA button (scale slightly on hover, warm glow shadow)
- Ensure the tagline/description text has gentle entrance feel if tw-animate-css supports it (fade-in-up or similar)
- Review spacing and typography for visual balance

**Landing page — audience-cards.tsx:**
- Add warm hover effect on cards: subtle warm border glow or warm background tint on hover
- Add smooth transition for hover state (transform + shadow + background)
- Ensure card icons use consistent warm or primary coloring (per earlier decision: no mixing green/warm for same-level elements)
- Add subtle warm accent to card backgrounds (very light warm tint)

**Landing page — stats-bar.tsx:**
- If stats are still placeholder "--" values, populate with real data by querying active job count, source count, and district count (or add a TODO comment if this requires a server component refactor)
- Add warm accent styling to stat numbers (warm amber color for the big numbers)

**Jobs page — job-row.tsx:**
- Add warm hover transition: instead of just `hover:bg-muted/50`, add a warm-tinted hover (`hover:bg-cta/5` or similar very subtle warm wash)
- Add smooth transition timing for the hover state change
- Subtle left-border accent on hover (2px warm amber left border that fades in)

**Jobs page — job-list.tsx:**
- Add warm styling to the count header text
- Polish the empty state: add a warm accent icon or illustration element

**Header — header.tsx:**
- Add subtle warm underline or indicator on the active nav link
- Ensure header backdrop-blur has slight warm tint in the background

**Test both modes:** Every warm accent must be tested in both light and dark. Use CSS custom properties scoped to `:root` vs `.dark` (existing pattern). Warm backgrounds that work on dark may be invisible on light — adjust lightness/opacity per mode.

Anti-patterns to avoid:
- Do NOT use inline `style` props for warm accents — use Tailwind classes or CSS custom properties
- Do NOT add framer-motion or heavy animation libraries — use CSS transitions and tw-animate-css
- Do NOT make warm accents too strong — they should be subtle enhancements, not overwhelming
  </action>
  <verify>
    <automated>cd "/Users/cciu/Documents/Claude Code Repository/school-job-portal" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>All pages have warm accent backgrounds, hover transitions, and micro-interactions. globals.css has warm utility classes. Both light and dark modes look polished with consistent warmth. The site feels more alive and less generic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and mobile verification</name>
  <action>
Visual polish pass across all pages: warm accents, hover effects, micro-interactions, responsive mobile experience (filter drawer, compact rows, auto-hide header), About page, and Coaching page.

Verify steps:
1. Start dev server: `npm run dev` (port 3003)
2. Visit http://localhost:3003 — check landing page warm accents, hover effects on cards and CTA
3. Visit http://localhost:3003/jobs — check desktop filter bar unchanged, hover effects on job rows
4. Open Chrome DevTools, toggle to mobile (375px width):
   - Search bar should be visible, "Filters" button present
   - Tap "Filters" — drawer slides up from bottom with all filters
   - Change a filter — count updates in drawer
   - Tap "Show X Results" — drawer closes
   - Scroll down — header should auto-hide
   - Scroll up — header reappears
   - Job rows show compact stacked layout
5. Visit http://localhost:3003/about — mission-driven content, warm styling, links to coaching
6. Visit http://localhost:3003/coaching — form with required/optional fields, warm styling
7. Toggle between light and dark mode — verify warm accents work in both
8. Check navigation includes "Coaching" link
  </action>
  <verify>Human visual inspection confirms all pages look polished in both modes and mobile is fully functional</verify>
  <done>User approves visual polish, mobile responsiveness, and all page content</done>
</task>

</tasks>

<verification>
- Build succeeds: `npx next build`
- Visual verification in both light and dark modes
- Mobile responsiveness tested at 375px, 768px, and 1024px+ viewports
- All warm accents visible but subtle in both modes
- Hover transitions smooth (no janky or instant state changes)
</verification>

<success_criteria>
The site feels alive, warm, and handcrafted. Warm amber accents appear as hover glows, background washes, and interactive state changes. Transitions are smooth. Both light and dark modes are polished. The "AI-generated" generic feel is replaced with visual personality.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-polish-static-pages/08-03-SUMMARY.md`
</output>
